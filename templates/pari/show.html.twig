{% extends 'base.html.twig' %}

{% block title %}Pari{% endblock %}

{% block body %}
    <h1>Pari</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pari.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pari_index') }}">back to list</a>

    <a href="{{ path('app_pari_edit', {'id': pari.id}) }}">edit</a>

    {{ include('pari/_delete_form.html.twig') }}
{% endblock %}
